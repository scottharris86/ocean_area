<div class="container">
<% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
<% end %>
<div class="row">
    <div class="col-xs-12">
        <div class="center-block order-form">
            <%= render 'form' %>
            <br />
            <div class="col-xs-5 col-xs-offset-7">
                <label class="pull-right">Total: $ <%= @cart.total %></label>
            </div>
        </div>
    </div>
</div>
</div>
