<div class="container">
<% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
<% end %>
    <div class="row">
        Order Confirmation Number: <%= @order.stripe_id %>
    </div>
</div>
